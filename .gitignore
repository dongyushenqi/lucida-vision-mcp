node_modules/
dist/
coverage/
*.tgz
*.log
.DS_Store
*.sqlite
*.sqlite-journal
.env
.env.local

# Compiled per-package build outputs (never committed).
# NOTE: do NOT use a bare "lib" here — it would also ignore the committed
# source file scripts/lib/bundle-server.mjs and break the release build.
/packages/*/lib/
