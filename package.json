{
  "name": "mcp-vision-server",
  "version": "0.1.0",
  "private": true,
  "description": "MCP Vision Server — 通用视觉感知基础设施（眼睛不是大脑：只陈述视觉事实，绝不做领域决策）",
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^5.6.3",
    "vitest": "^3.2.4"
  }
}
