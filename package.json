{
  "name": "lucida-vision-mcp",
  "version": "0.1.3",
  "private": true,
  "description": "lucida-vision-mcp — 通用视觉感知 MCP 基础设施（眼睛不是大脑：只陈述视觉事实，绝不做领域决策）",
  "packageManager": "pnpm@11.21.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "archiver": "^8.0.0",
    "esbuild": "^0.28.2",
    "typescript": "^5.6.3",
    "vitest": "^3.2.4"
  }
}
